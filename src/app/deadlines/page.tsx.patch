--- src/app/deadlines/page.tsx
+++ src/app/deadlines/page.tsx
@@ -4,6 +4,8 @@
 import React, { useState, useEffect } from "react";
 import { DeadlineWidget } from "@/components/DeadlineWidget";
 import { Plus, X } from "lucide-react";
+import { db } from "@/lib/firebase";
+import { collection, onSnapshot, doc, setDoc, deleteDoc } from "firebase/firestore";
 
 interface Deadline {
   id: string;
@@ -17,41 +19,12 @@
   const [newTime, setNewTime] = useState("");
 
-  const loadDeadlines = () => {
-    const saved = localStorage.getItem("deadlines");
-    if (saved) {
-      setDeadlines(prev => JSON.stringify(prev) === saved ? prev : JSON.parse(saved));
-    } else {
-      const initial = [];
-      setDeadlines(initial);
-      localStorage.setItem("deadlines", JSON.stringify(initial));
-    }
-  };
-
   useEffect(() => {
-    loadDeadlines();
-    window.addEventListener("storage", loadDeadlines);
-    window.addEventListener("local-storage-sync", loadDeadlines);
-    return () => {
-      window.removeEventListener("storage", loadDeadlines);
-      window.removeEventListener("local-storage-sync", loadDeadlines);
-    };
+    const unsub = onSnapshot(collection(db, "deadlines"), (snapshot) => {
+      const loaded: Deadline[] = [];
+      snapshot.forEach(doc => loaded.push({ id: doc.id, ...doc.data() } as Deadline));
+      setDeadlines(loaded);
+    });
+    return () => unsub();
   }, []);
 
-  useEffect(() => {
-    if (deadlines.length > 0) {
-      const savedStr = localStorage.getItem("deadlines") || "[]";
-      const currentStr = JSON.stringify(deadlines);
-      if (savedStr !== currentStr) {
-        localStorage.setItem("deadlines", currentStr);
-        window.dispatchEvent(new Event("local-storage-sync"));
-      }
-    }
-  }, [deadlines]);
-
-  const handleAdd = (e: React.FormEvent) => {
+  const handleAdd = async (e: React.FormEvent) => {
     e.preventDefault();
     if (!newTitle || !newDate || !newTime) return;
     
+    const newId = Date.now().toString();
     const newDeadline = {
-      id: Math.random().toString(36).substring(7),
+      id: newId,
       title: newTitle,
       targetDate: `${newDate}T${newTime}:00`
     };
     
-    setDeadlines(prev => [...prev, newDeadline]);
+    await setDoc(doc(db, "deadlines", newId), newDeadline);
     setIsModalOpen(false);
@@ -62,5 +35,5 @@
 
-  const handleDelete = (id: string) => {
-    setDeadlines(prev => prev.filter(d => d.id !== id));
+  const handleDelete = async (id: string) => {
+    await deleteDoc(doc(db, "deadlines", id));
   };
 
